version: '3'
services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - mockserver
    environment:
      - MOCK_SERVER_URL=http://mockserver:1080
  mockserver:
    image: mockserver/mockserver:5.15.0
    ports:
      - "1080:1080"
networks:
  default:
    driver: bridge