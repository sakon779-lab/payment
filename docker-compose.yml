version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
      - ./tests:/app/tests

  mockserver:
    image: mockserver/mockserver:5.14.0
    ports:
      - "5000:1080"